# Documentation Components

Placeholder. The vocabulary and the doctrine — the declarable genres, the registry format, what
a component's home means — are defined together with the registry format itself, and replace this
file wholesale.
