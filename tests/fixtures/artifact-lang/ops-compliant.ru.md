# Ops Checklist — 042-promo-delivery

> Generated by `ops-checklist`. Mark each item Applicable / N/A (reason) / Pending.

## Release & rollout
- [x] Feature flag wraps the new feature (kill switch ready) — `Features.promoOnGoods`
- [x] Minimum supported OS version covered — `CartScreenTest.minimumOS`
- N/A: Gradual rollout plan documented (1% → 10% → 100% with monitoring gates) — исправление расчёта скидки выходит сразу для всех, отдельного плана раскатки у него нет.
- [ ] Pending: Feature flag TTL set (prevent dead-flag accumulation)

## State & lifecycle
- [x] App lifecycle handled — foreground, background, suspend, terminate — `CartViewModel.onResume`
- [x] Single source of truth for feature state (no mutation from multiple owners) — `CartViewModel.state`
- [x] All four UI states covered — loading, success, error, empty — `CartScreen` preview
- [x] Background → foreground refresh policy decided — `CartRefreshPolicy`

## Networking
- N/A: API endpoints versioned or contract stable — скидка считается на устройстве, и новых запросов к серверу изменение не добавляет.
- N/A: Retry policy idempotency-aware — сетевых вызовов в этой задаче нет, поэтому повторять нечего.

## Testing
- [x] Unit tests for domain / service / repository — `DiscountPolicyTest`
- [x] Snapshot tests for critical UI states (all four: loading / success / error / empty) — `CartScreenSnapshotTest`
- [x] Integration tests at layer boundaries — `CartRepositoryIntegrationTest`
- [x] UI tests for golden path only (expensive — don't over-invest) — `CheckoutGoldenPathUITest`
- [x] Manual test pass: accessibility + device matrix — `ManualChecks.md`
